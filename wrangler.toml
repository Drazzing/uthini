name = "uthini"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# Pages cannot use send_email — call the email Worker instead
[[services]]
binding = "EMAIL_WORKER"
service = "uthini-contact-email"
